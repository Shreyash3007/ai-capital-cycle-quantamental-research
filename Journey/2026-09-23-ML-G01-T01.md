# ML-G01-T01 session checkpoint: 2026-09-23

## Observed

- The learner gave `1.4` and `1.8` for revenue growth `0.20` and `0.40`, with weight `2.0` and bias `1.0`.
- Both hand predictions are correct: `2.0 * 0.20 + 1.0 = 1.4` and `2.0 * 0.40 + 1.0 = 1.8`.
- The learner answered `X.shape == (2, 1)` for two rows and one feature. This is correct; the shapes of `w` and the prediction vector were not answered yet.
- The formula and inputs were supplied in the task; no arithmetic hint or finished Python solution was given.

## Still open

- The learner has not yet explained the shapes of `w` and the prediction vector or why the dimensions align.
- No Python implementation, run, changed-input check, or failure experiment has been observed.
- T01's first acceptance check is partial, so task, phase, and overall progress remain `0%`.

## Next action

Ask for the shapes of `w` and the two predictions and why they align with `X.shape == (2, 1)`. Then have the learner write and run the first Python step in [the empty learner file](../Projects/AI%20Capital%20Cycle%20Quantamental%20Research%20Engine/src/01_vectorized_prediction.py).
